# See Our Family - Environment Configuration
# Copy this to .env and edit for your environment.
# NEVER commit .env to git.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=seeourfamily
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Media storage directory (outside the web root for security).
# New uploads go here as flat UUID-based files: {MEDIA_DIR}/{family_id}/{uuid}.ext
# Legacy files under /Gene/File/{FamilyName}/ are still resolved via MEDIA_LEGACY_DIR.
# MEDIA_DIR=/var/media/seeourfamily
# MEDIA_LEGACY_DIR=/var/www/html

# Set to 1 for development: enables "Dev login" buttons on the login page
# that bypass password authentication. NEVER enable in production.
# APP_DEV=1

# SMTP for outgoing emails (password resets, invitations).
# Leave blank to display tokens on-screen instead of emailing.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@example.com
# SMTP_FROM_NAME=See Our Family
